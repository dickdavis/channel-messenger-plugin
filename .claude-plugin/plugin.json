{
  "name": "channel-messenger",
  "version": "0.1.0",
  "description": "Channel plugin that bridges Claude Code with a messaging API via WebSocket",
  "author": {
    "name": "Dick Davis"
  },
  "channels": [
    {
      "server": "messenger",
      "userConfig": {
        "host": { "title": "Host", "description": "API base URL (e.g. https://api.example.com)", "type": "string", "sensitive": false },
        "token": { "title": "Token", "description": "Authentication token for the messaging API", "type": "string", "sensitive": true }
      }
    }
  ]
}
